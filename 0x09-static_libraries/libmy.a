#include <stdio.h>
#include <stdlib.h>

int main()
{
    int result = 0;

    result = add(3, 5)
    printf("Result = %d", result);

    getchar();
    return 0;
}    

int add(int a, int b)
{
    int sum = 0;

    sum = a = b;

    return sum;
}
